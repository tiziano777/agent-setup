services:

  # ==========================================
  # LiteLLM Proxy - LLM API Key Rotator
  # ==========================================
  litellm-proxy:
    image: ghcr.io/berriai/litellm:main-latest
    container_name: litellm-proxy
    ports:
      - "4000:4000"
    volumes:
      - ./proxy_config.yml:/app/config.yaml:ro
    env_file:
      - .env
    command:
      - "--config"
      - "/app/config.yaml"
      - "--port"
      - "4000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:4000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # ==========================================
  # Qdrant Vector Database
  # ==========================================
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant
    ports:
      - "6333:6333"   # REST API
      - "6334:6334"   # gRPC
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:6333/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # ==========================================
  # PostgreSQL + pgvector
  # ==========================================
  postgres-vector:
    image: pgvector/pgvector:pg16
    container_name: postgres-vector
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: vectors
    volumes:
      - pgvector_data:/var/lib/postgresql/data
      - ./deploy/docker/init-phoenix-db.sql:/docker-entrypoint-initdb.d/init-phoenix-db.sql:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  # ==========================================
  # Arize Phoenix - LLM Observability
  # ==========================================
  phoenix:
    image: arizephoenix/phoenix:latest
    container_name: phoenix
    ports:
      - "6006:6006"    # UI + OTLP HTTP
      - "4317:4317"    # OTLP gRPC
    environment:
      PHOENIX_SQL_DATABASE_URL: postgresql://postgres:postgres@postgres-vector:5432/phoenix
      PHOENIX_ENABLE_PROMETHEUS: "false"
    depends_on:
      postgres-vector:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:6006/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  # ==========================================
  # Streamlit App (da definire)
  # ==========================================
  # app:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   container_name: streamlit-app
  #   ports:
  #     - "8501:8501"
  #   depends_on:
  #     litellm-proxy:
  #       condition: service_healthy
  #   env_file:
  #     - .env
  #   restart: unless-stopped

volumes:
  qdrant_data:
  pgvector_data:
